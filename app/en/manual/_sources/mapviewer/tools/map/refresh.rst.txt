Refreshing the Map
===================

This tool rebuilds the map and all included map services.

Ideally, this tool should never need to be used, because the refresh happens automatically after every
change to the map view or when a topic layer is switched on.

However, if errors occur with the internet connection and maps are therefore not reloaded,
this can also be forced afterwards with this tool.
