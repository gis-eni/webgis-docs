=========================
Windows-Authentifizierung
=========================

Damit die **Windows-Authentifizierung** verwendet wird, muss in der ``portal.config`` der Schlüssel ``security`` bzw. ``security_allowed_methods`` das **Schlüsselwort** ``windows`` enthalten.
